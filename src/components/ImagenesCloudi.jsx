import { Container } from 'reactstrap';
import Dropzone from 'react-dropzone';
import {useState} from 'react';

const ImagenesCloudi = (props) => {
    const [image, setImage] = useState()

  return (
    <div className='mt-64'>
      <Container>
      
      </Container>
    </div>
  )
}

export default ImagenesCloudi
